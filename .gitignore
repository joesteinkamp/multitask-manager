# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcscmblueprint
xcuserdata/
*.moved-aside
*.pbxuser
!default.pbxuser

# Swift Package Manager
.swiftpm/
.build/
# Scratch paths for verifying against a second Swift toolchain. Building a
# different toolchain into the same .build mixes artifacts and produces link
# errors, so those runs need their own directory — and it must not be committed.
.build-*/

# SwiftPM build products
Packages/*/.build/

# Screenshots dropped here to show the agent what the UI actually looks like.
# Deliberately untracked: they are a conversation aid, not project history, and
# a screenshot is the fastest way to settle a UI bug that reasoning cannot.
temp-screenshot-share/
